# 数独游戏 Vibe Coding 实验报告

**项目名称**：终端数独游戏（Python 3.13）  
**实验者**：XXX  
**日期**：2026年7月7日  
**开发方式**：Vibe Coding（AI 辅助编程）

---

## 一、实验背景与目标

本次实验旨在通过“Vibe Coding”模式（即以自然语言与 AI 对话，逐步生成、修改和完善代码）快速构建一个可玩的数独游戏。目标不仅仅是实现功能，更是体验 AI 辅助编程的协作流程，评估其在算法设计、界面交互、调试优化等环节的实际效果。

---

## 二、Vibe Coding 过程记录

### 1. 需求定义阶段（对话式）
我向 AI 描述了基本需求：“用 Python 写一个命令行数独游戏，要有难度选择、提示功能和随机生成关卡。”  
AI 迅速给出了初步的代码框架，包括 `generate_board()`、`solve()`、`display()` 等函数原型，并建议使用 `random` 和 `copy` 模块。

### 2. 核心算法迭代
- **生成终盘**：初始实现采用简单回溯填充，但偶尔陷入死循环。我反馈“生成速度慢且不稳定”，AI 建议采用“从已知有效终盘随机置换数字和行列”的混合方法，显著提升生成效率。
- **挖空与难度**：根据难度定义挖空数量（简单 30 空，中等 40 空，困难 50 空，专家 58 空）。AI 自动加入了唯一解校验，确保每个谜题都有且仅有一个解。
- **求解器优化**：初始求解递归深度过大，AI 推荐加入“最少候选数优先”启发式，将求解时间从 0.5 秒压缩到 0.01 秒以内。

### 3. 交互界面设计
我要求“不要图形库，只用终端，但要好看且有颜色”。AI 提供了 ANSI 转义码方案，并设计了光标移动、数字输入、错误高亮等逻辑。过程中我多次对按键响应和光标位置反馈提出调整，AI 均能准确定位代码并修改。

### 4. 调试与边界处理
- 遇到输入非数字字符导致崩溃，AI 添加了异常捕获和输入过滤。
- 提示功能初期会覆盖用户已填数，修改为仅当格子为空或用户填错时才给予提示。
- 重置功能（R 键）的保留原谜题但不重置提示次数，AI 通过状态分离解决。

### 5. 代码组织与重构
在功能基本完善后，我请 AI 将代码拆分为 `main.py`（界面和交互）、`sudoku.py`（算法核心）、`constants.py`（配置常量），并添加详细的注释和类型注解，提升可维护性。

---

## 三、最终成果

- 完成了完整可运行的数独游戏，所有核心功能稳定。
- 代码量约 350 行（含注释），结构清晰。
- 通过 20+ 个手工测试用例（包括极端填数、快速连续按键、切换难度），未发现逻辑错误。

---

## 四、实验心得与反思

### 1. Vibe Coding 的优势
- **快速原型**：从零到可玩版本仅用约 20分钟，传统编码至少需要半天。
- **降低认知负担**：AI 能即时补全算法细节（如回溯模板、候选数优化），让我能聚焦在高层次设计上。
- **良好的纠错体验**：遇到 Bug 时，只需贴上报错信息，AI 能给出定位和修复建议，效率极高。

### 2. 需要注意的挑战
- **生成质量依赖提问精确性**：模糊的需求会导致 AI 生成平庸或偏离预期的代码，需要多次澄清。
- **过度依赖风险**：有些代码片段我未能完全理解就合并，后续调试时发现隐患。必须保持对核心逻辑的审阅。
- **AI 的“幻觉”**：曾建议使用不存在的 Python 库函数，幸好通过测试及时发现。
